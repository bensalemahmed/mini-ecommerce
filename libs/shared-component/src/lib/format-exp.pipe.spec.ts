import { FormatExpPipe } from './format-exp.pipe';

describe('FormatExpPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatExpPipe();
    expect(pipe).toBeTruthy();
  });
});
